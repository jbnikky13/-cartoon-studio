# Cartoon Studio V5

Lightweight 2D cartoon animation app using the supplied character artwork.

## Structure

- `app.py` - Streamlit application
- `requirements.txt` - Python dependencies
- `assets/characters/` - character artwork

The V5 architecture does not require Blender or an OpenGL/EGL context.
